---
状态: open
级别: L1
发现: YYYY-MM-DD
模块: <词表之一，见 .agents/workflow-modules.txt>
---
# INCIDENT — <日期 主题>
<!-- 复制为 YYYY-MM-DD-<主题>.md；线上或实测发现的功能缺陷都记录，历史 BUG 可回填 -->
<!-- frontmatter 受限子集（2026-09-13）：状态∈open/fixed/closed（严格枚举，附注写备注键）；级别∈L0/L1/L2/L3；闭环引擎启用前的回填件加一行 `流程: legacy`（豁免级别/配对检查）；正文不再写状态/级别/发现行 -->

## 时间线
- <发现 → 定位 → 止血 → 恢复，带时刻>

## 影响面
- <页面 / 接口 / 数据范围>

## 根因
<一句话；深层原因另起一行>

## 为什么之前没拦住
<门禁 / 测试 / 规范各查一遍：哪一层本可以拦住>

## 复盘三件套（缺一不可）

> 目的：让事故真正回到 Plan，不让「防复发」承诺停留在文档里。模块已逐步上线，事故可能来自 dev / staging / prod 任一环境。

1. 结构性修复
   - 修复 commit：<SHA + 改动文件清单>
   - 影响环境：dev / staging / prod（已上线模块若波及 prod，需附影响用户数 / 持续时长 / 是否回滚）
   - 是否需要新 intent：
     - 是 → ../intents/YYYY-MM-DD-<主题>.md（根因属「门禁缺位 / 规范未落地 / 系统性问题」时强制立，即使结构性修复已完成）
     - 否 → 理由：<根因属实现 bug、单点修复已完成，且已有规范条目覆盖>

2. 防复发验证（必须落到自动化用例或回归清单条目，禁止只写「已人工验证」）
   - 自动化用例：项目测试套件 <文件>（<用例名 / 数量>）
   - 或 test 步骤：.agents/commands/test.md + <具体操作>
   - 或回归清单条目：workflow/regression-checklist.md（防复发验证节追加一行）

3. 规范条目（必须有可追溯的落点）
   - 落点（多选）：AGENTS.md <章节> / .agents/hooks/<脚本> / .agents/skills/<skill> / .agents/commands/<cmd> / 回归清单 §<章节>
   - 引用：commit <SHA> / 文件:<路径>#L<行>
   - 若无新增条目，理由：<已有条目覆盖，说明条目位置>
