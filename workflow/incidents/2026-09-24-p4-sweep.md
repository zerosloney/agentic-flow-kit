---
状态: fixed
级别: L1
发现: 2026-09-24
模块: pipeline
备注: 全项目审查遗留的 P4 备忘项批量清账（承接 2026-09-24-review-fixes，同日第二单）；含上轮遗漏的 add-host 跳过分支告警。
---
# INCIDENT — 审查 P4 备忘清账：正则收窄、豁免面收紧、钩子匹配加固、fail-closed 补口

## 时间线
- 2026-09-24：review-fixes 销单后用户指示继续处理审查遗留备忘项；同日批量修复 13 项 + 探针验证 + npm test 全绿落库

## 影响面
- H1 前缀截断正则含裸连字符（kb-search / 看板 / INDEX 生成器三处）：kebab-case 标题会被截成末段（潜伏，当前语料未触发）
- gen-workflow-index END 锚点被截断时静默追加第二段生成区；commit-msg 任意 merge/revert 开头 + 空首行均可豁免类型校验
- trae 宿主：deny/ask 裸子串匹配可被 `git -c x push --force` 绕过、`--force-with-lease` 被误拦；check-architecture.sh 未装时误报「发现违例」；PATH 回退可能选中 WSL bash
- dotnet-ca：python 运行期报错时静默放行；红线 4.3 漏检「{ 换行」与 expression-bodied 属性
- add-host 跳过文件不入台账且无提示，后续 sync 永不升级

## 根因
四类小缺陷同源：①把字面量数据当正则/裸子串用（H1 截断、rule-budget 前缀、trae 匹配）；②豁免/白名单写成宽松通配（commit-msg）；③异常分支不分流（ENOENT≠违例、python 报错≠无违例）；④静默继续优先于 fail-loud（INDEX 双生成区）。

## 为什么之前没拦住
- P4 备忘项多为「潜伏雷 / 天花板」性质，首轮修复聚焦 P1-P3 主链；钩子类（trae/dotnet-ca）无 fixture 测试基建，裸子串与正则边界靠人工推演未被发现

## 复盘三件套（缺一不可）

1. 结构性修复
   - 修复 commit：554f437（改动清单见 ../plans/2026-09-24-p4-sweep.md；装副本经 flow-kit sync 落地）
   - 影响环境：dev（v0.2.0 已发布版仍带缺陷，下次发版修复）
   - 是否需要新 intent：
     - 否 → 理由：根因属实现缺陷四类小项，均已单点修复并经探针/测试验证；无规范缺位

2. 防复发验证（必须落到自动化用例或回归清单条目，禁止只写「已人工验证」）
   - 自动化用例：gen-workflow-index.test.mjs 场景 5/6（kebab-case H1 完整保留 / END 锚点残缺 exit 1）；agg-delegations.test.mjs 场景 1（未知结果单列断言）
   - 回归清单条目：workflow/regression-checklist.md（防复发验证节已追加）
   - 常驻哨兵：commit-msg 收紧后随本仓库每次提交自动重验；trae/dotnet-ca 为装户侧文件，探针脚本与判据记录于本单时间线可复跑

3. 规范条目（必须有可追溯的落点）
   - 落点：workflow/regression-checklist.md §防复发验证（「字面量匹配优先于把用户数据当正则；异常分支先分流再报」两条口径）
   - 引用：本 incident + ../plans/2026-09-24-p4-sweep.md；commit 554f437
