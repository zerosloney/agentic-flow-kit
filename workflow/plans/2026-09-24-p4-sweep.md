---
状态: done
级别: L1
日期: 2026-09-24
模块: pipeline
---
# PLAN — 审查 P4 备忘清账：正则收窄、豁免面收紧、钩子匹配加固、fail-closed 补口

对应入口：../incidents/2026-09-24-p4-sweep.md

## 改动面
- `templates/_agents/scripts/{kb-search,workflow-board-server,gen-workflow-index}.mjs`：H1 前缀分隔正则收窄为「em/en dash 任意间距 或 带空格连字符」——裸连字符属 kebab-case 不截断（三处同款）。
- `templates/_agents/scripts/gen-workflow-index.mjs`：BEGIN/END 锚点残缺或顺序颠倒 → exit 1 未写盘（原静默 append 产生双生成区）；测试补场景 5/6。
- `templates/_agents/scripts/agg-delegations.cjs`：未知结果（结果列拼错）与待修单列计数，不再混算；parseTableRows 冗余 filter 清理；测试场景 1 补未知口径断言。
- `templates/_githooks/commit-msg`：豁免收紧为真实 merge/revert 形态白名单（Merge …/Revert …/merge:/revert:），空首行 fail-closed。
- `templates/_githooks/pre-push`：修正与头部说明矛盾的「CI 服务端兜底」注释（本钩子是唯一机器门）。
- `templates/_agents/scripts/rule-budget.sh`：目录条目暂存命中判定改 case 字面前缀（pat 含 . 不再当正则）。
- `templates/_agents/hooks/check-pairing-incremental.sh`：补「读工作区而非暂存 blob」已知边界注释。
- `modules/hosts/trae/hooks/pre-shell-check.cjs`：gateMatch（token 边界 + 剥 `-c/-m` 全局参后匹配）——堵 `git -c x push --force` 绕过、免 `--force-with-lease` 误拦与消息字样误触发；「六项门禁」过时注释改通用包口径。
- `modules/hosts/trae/hooks/post-edit-check.cjs`：ENOENT 分流为「检查未执行」（不再误报发现违例），对齐 pre-shell-check 口径。
- `modules/hosts/trae/hooks/resolve-shell.cjs`：PATH 回退经 where.exe 取首个非 System32 命中，避开 WSL bash。
- `modules/gates/dotnet-ca/check-architecture.sh`：python 运行期报错 fail-closed；红线 4.3 改全文 finditer（支持 `{` 换行与 expression-bodied，先剥注释防误报）；红线 5 补已知天花板注释。
- `src/add-host.mjs` + `README.md` + `src/cli.mjs`：跳过文件不入台账的语义显式告警 + 文档化（纳入包管理须 --force）。
- `templates/_agents/scripts/verify-wiki-consistency.mjs`：静态 href 扫描补意图注释（守的是手工编辑看板的 owned 内容）。
- `workflow/regression-checklist.md`：防复发验证节追加条目。

## 验证方式
- 静态门：`npm test` 全套件全绿（170 断言，含新增 gen-workflow-index 场景 5/6 与 agg 未知口径断言）。
- 探针对账：commit-msg 五用例（merged 拦 / Merge·Revert·merge: 豁免 / 空拦）；gateMatch 四用例（绕过变体拦 / lease 与消息字样放行 / 直拦）；dotnet-ca 4.3 fixture 恰中两种新形态且注释免疫；resolve-shell 正常解析 Git Bash；rule-budget --all/--staged 通过；force-with-lease 零 deny。

## 确认与复核
- 确认结果：approved（2026-09-24 用户对话内确认「继续处理」遗留备忘项）；done（2026-09-24 关单：npm test 170 断言全绿 + 六组探针对账通过，修复 commit 554f437 已回填 incident）
