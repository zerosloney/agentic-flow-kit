---
name: 示例-并行实现评审
description: 两域并行实现 → 汇聚过测试门 → 部署验证（step）→ 独立评审（示例，按 plan 改写后使用）
concurrency: 2
---
# workflow — 示例：并行实现评审

> 格式示例：三形态各一处（role 派单 / gate / step）；id / after / task 按 plan 改写；随 plan 确认后方可执行。机制口径见 `_TEMPLATE.md`。

| id | after | role | step | task / params | files（授权） | accept（验收判据） | gate | retries |
|----|-------|------|------|---------------|--------------|-------------------|------|---------|
| backend | — | implementer | — | 实现后端改动（示例占位，按 plan 改写） | src/api/** | 项目测试命令通过 | | 0 |
| frontend | — | implementer | — | 实现前端改动（示例占位，按 plan 改写） | web/** | 项目构建命令通过 | | 0 |
| gate-all | backend,frontend | — | — | — | — | — | npm test | |
| deploy-check | gate-all | — | 示例-部署验证 | env=staging | | 部署命令与冒烟 exit 0 | | 0 |
| review | deploy-check | independent-reviewer | — | 独立评审两域改动是否符合 plan 与验收判据 | | 符合 plan 与判据 | | 0 |
