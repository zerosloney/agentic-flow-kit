---
description: Maintain 阶段 · 立 incident + 复盘三件套（incident 在 L1+ 即 intent 等价物）
---

严格按 AGENTS.md 与 .agents/commands/maintain.md 执行：Maintain 阶段 · 立 incident + 复盘三件套（incident 在 L1+ 即 intent 等价物，不必另造 intent）。

先完整读取上述两个文件并完全遵循，读不到即停下报告阻塞；本文件不内联步骤副本，避免与权威版本漂移。

【OpenCode 宿主注记】
- 确认门：止血方案由用户拍板（AI 只执行选定方案）；三件套草稿经用户确认后才落地。
- 配 plan 的时点与形态以权威文件为准——incident≡intent 免掉的是 intent，plan 不可免（commit / push 两处门禁按同名硬拦）。
- 复现路径与验收明确的局部热修可委派 `implementer`（`.opencode/agents/implementer.md`）；事故诊断、根因定性与三件套起草不委派。
